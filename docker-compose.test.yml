services:
  postgres-test:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: testllm_test
      POSTGRES_PASSWORD: testllm_test
      POSTGRES_DB: testllm_test
    tmpfs:
      - /var/lib/postgresql/data
